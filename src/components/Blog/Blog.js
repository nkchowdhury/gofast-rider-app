import React from 'react';
import Header from '../Header/Header';

const Blog = () => {
    return (
        <div style={{textAlign:'center'}} className="container">
        <Header></Header>
            <h1>Blog page is coming soon</h1>
        </div>
    );
};

export default Blog;